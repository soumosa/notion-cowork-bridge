# Development example only. Keep the real token in a secret manager.
MCP_AUTH_TOKEN=replace-with-at-least-32-random-characters
MCP_WORKSPACE_ROOT=/absolute/path/to/notion-workspace
MCP_PORT=3210
# Exact tunnel hostname only; omit https:// and /mcp.
MCP_ALLOWED_HOSTS=example-name.ngrok-free.dev
